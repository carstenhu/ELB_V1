name: CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  placeholder-checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Define project checks
        run: |
          echo "Replace this placeholder with your real lint/build/test commands."
          echo "This workflow exists so branch protection can target a single clear CI job."
